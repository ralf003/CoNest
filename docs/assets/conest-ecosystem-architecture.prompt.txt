Asset: conest-ecosystem-architecture.png
Method: built-in image_gen, new image; no CLI fallback.
Reference: approved-platform.png from the architecture-diagrams skill was inspected for visual style only. Its architecture and terminology are not inputs to this design.

Use case: infographic-diagram.
Create a polished, publication-quality 2D technical architecture diagram for CoNest, an entire plugin ecosystem platform, not merely an agent-tool bridge. Landscape 3:2, high-resolution, clear English text. No overall title inside the image; the Markdown document supplies the title. Exact module text below.

Layout and boundaries:
1. Top strip with three equally sized white source cards: "OpenClaw ecosystem", "DSH ecosystem", "Service packages". A slim header over the strip: "Plugin sources". Each source means whole packages containing multiple contributions, not just tools.
2. One full-width outlined region "CoNest Manager · Control plane" containing four aligned white cards with selectively colored header bars: "Catalog & artifacts" (small text: "Identity / versions / provenance"), "Host profiles" ("Dependencies / compatibility"), "Lifecycle planner" ("Install / upgrade / recovery"), "Policy & state" ("Authorization / desired & observed state"). Add a compact "Studio / CLI" client card at the left inside this same region, visually distinct from the Manager service cards; a small rightward arrow labelled "Manage" points to the service area. These are related control functions, not a serial execution pipeline: do NOT put arrows between the four service cards. Source strip connects downward only to Catalog & artifacts with a single branch connector labelled "Discover packages".
3. Below it one full-width region "Execution plane · Native hosts" with four equal-width aligned white host columns, each its own outlined boundary. Each has a top narrow box "Host Connector" and beneath it its native components:
   "OpenClaw Host" in a sky blue header. Body: "Native plugins", "Channels / sessions", "Tools / providers".
   "DSH Host" in a fresh green header. Body: "Native plugins", "Agents / workflows", "Session / UI services".
   "CoNest Cordis Host" in a warm yellow header. Body: "Service components", "Dependency lifecycle", "Shared state owners".
   "External Service Hosts" in a coral header. Body: "MCP / HTTP", "Managed processes", "Native adapters".
   Under the four hosts, inside the Execution plane region, a thin outlined band "Typed interaction contracts" with smaller text "Service / Tool · Event · Job · Executor · UI". A short bidirectional line connects each host to this band. These are peer hosts; no arrow should suggest sequential execution or that OpenClaw contains DSH.
4. At bottom a full-width white resource region "Owned resources" with four small boxes "Plugin state", "Secrets & config", "Workspace data", "UI assets". One labelled downward connection from execution-plane band to resource region: "Scoped access".
5. Between control and execution plane, use a blue branching DOWNWARD arrow from Manager to each Host Connector, labelled "Plans / configuration". On the far right a clearly separate green dashed UPWARD return arrow from execution plane into Manager labelled "Observed state / health". The return arrow must terminate in Manager. Manager manages lifecycle and policy, not every runtime tool invocation. The interaction band is inside the execution plane, not the manager. Render small legend: "Blue: management   Green dashed: observation   Black: runtime interaction".

Style:
White canvas, predominantly white interiors. Crisp fine dark outlines, aligned full-width outer boundaries and equal peer card widths, generous internal spacing, black readable typography. Selective flat sky blue #75C7EE, green #A5DCB8, yellow #F5DA88, coral #EFAFA3 header fills; mostly white body. No gradient, shadow, 3D, gray text, faux dashboard controls, decorative icons, company logos or invented components. Make it visually sophisticated through hierarchy, alignment and restrained color. Preserve the exact labels, distinct parallel hosts, connector placement and arrow directions.

Revision 1: style and source-routing correction, using the first generated image as the edit target.
Edit this architecture diagram. Preserve every label, module, boundary and layout. Make these exact corrections: 1) Remove ALL dark fog, colored glow, lighting effects, shadows, gradients, tinted canvas and tinted module bodies. The canvas and ALL interior bodies must be SOLID PURE WHITE (#FFFFFF), including bottom resource cards. Use flat light sky-blue/green/yellow/coral color ONLY in the narrow existing header bars. Black text and thin dark outlines. This must look like a crisp flat technical diagram on white paper, never a translucent glass dashboard. 2) At the top connect ALL THREE source cards (OpenClaw ecosystem, DSH ecosystem, Service packages) by short downward lines merging into a single blue line ending at Catalog & artifacts, label Discover packages; do not imply DSH is the only source. 3) Change only the downward Scoped access arrow and its label to BLACK, matching runtime interaction, while management arrows remain blue and the health return remains green dashed. Preserve the four parallel native hosts and every other relationship. No new nodes, no changed text. Produce polished legible 2D diagram with majority white pixels.

Revision 2: text-only correction, using the white-background revision as the edit target.
undefined

Final validation: checked the three source connections, four peer native hosts, downward management paths, upward observed-state path, runtime contracts and scoped resource access. Corrected OpenClaw session label and observation legend.
Final PNG SHA256: 550c659d7c5020dd31706433fbe5066fb886644a0f3888fb0702b42f3be37641
