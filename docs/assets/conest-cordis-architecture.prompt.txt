Asset: conest-cordis-architecture.png
Revision: 2026-09-17, default two-process deployment.
Method: built-in image_gen edit and one endpoint correction; no CLI fallback or programmatic image editing.
Input roles: previous project architecture PNG is the edit target and style reference. The inspected approved-platform.png supplies style only, not architecture.
History: previous project asset and prompt are preserved in Git commit 9b581b1.

Containment:
- Process 1: OpenClaw Gateway includes native functions and CoNest Connector.
- Process 2: CoNest Host includes Management and Cordis Runtime.
- Cordis Runtime includes Core, enhancement components and optional ecosystem packs.
- DSH Agent / Session / Agent Loop / models / tools form a composition inside the optional pack, not a mandatory third process.
- Studio / CLI is a client outside both application processes.

Relationships:
- Studio / CLI -> Management: management requests.
- Management -> Runtime: internal configuration and lifecycle control.
- Connector <-> Runtime: calls, callbacks and events, bypassing Management.
- Core and loaded components use containment, not sequential execution arrows.
- External supervision and tool subprocesses are noted separately.

Initial restructuring prompt:
Use case: infographic-diagram.
Asset: replacement architecture diagram for CoNest DESIGN-zh.md.
Input image is the CURRENT DIAGRAM TO RESTRUCTURE, also a visual style reference only. Replace its topology with this complete specification. Keep its flat white, black-outline, selective sky-blue / green / yellow / coral header style, but no gradients or shadows. English labels. Landscape high resolution, legible text, no overall title.

Architecture: Default deployment contains EXACTLY TWO persistent application processes: OpenClaw Gateway and CoNest Host. Management and Cordis Runtime are logical modules INSIDE THE SAME CoNest Host process. DSH Agent Loop, Session, models, and tools are an optional service composition INSIDE Cordis Runtime, NOT a separate process. The diagram expresses TARGET architecture.

Layout: narrow left column (18% width) for a single client box "Studio / CLI" and broad right stack (77% width), equal aligned outer widths.
Top right process boundary: sky-blue header "Process 1 · OpenClaw Gateway". Inside one white row "Native Loop / sessions / plugins / channels". Below it white box "CoNest Connector" and subtitle "Host Services ↔ Contributions". One short double-headed line connects native row to Connector.
Lower right LARGE process boundary: sky-blue header "Process 2 · CoNest Host". Inside its top thin logical module, yellow header "Management module"; white body text "Packages & config · Lifecycle plans · State & recovery".
Below Management, with sufficient gap for a clearly labeled arrow, is a larger logical module with green header "Cordis Runtime". Inside Runtime:
1. white outlined box "CoNest Core" with subtitle "Context / Loader · Dependency graph · Scopes · Versioned lifecycle".
2. white outlined box with small green header "OpenClaw enhancement components"; white body has four concise labels in two columns: "Knowledge & context", "Policy & providers", "Events & workflows", "UI & backend".
3. dashed-outline group with narrow coral header "Optional ecosystem packs". Two peer white boxes within: broad "DSH Support Pack" with two lines "Agent / Session / Agent Loop" and "Models / Tools / Services / UI adapters"; narrower "Other ecosystem packs" with subtitle "MCP / HTTP / other adapters".
All three boxes are visibly INSIDE Cordis Runtime, itself INSIDE CoNest Host.
Bottom of Host interior a compact white note "Core mode runs without DSH. DSH composition reuses this process."

Exact edges:
- Studio / CLI -> Management module: thin blue arrow, label "Manage". Client outside both process boundaries.
- Management module -> Cordis Runtime: thin blue arrow across gap, label "Configure / activate / release". Internal control call, not execution route.
- CoNest Connector <-> Cordis Runtime: BLACK DOUBLE-HEADED line labelled "Calls / callbacks / events"; route in clear outside RIGHT gutter from Connector right side, down past Management, into Runtime right side. It MUST BYPASS Management, never go through Management or terminate there.
- No other arrows required. Do not fabricate sequential flows among the core, enhancement components and optional packs; containment shows composition.
Below the two-process drawing, a simple unboxed small note "External supervision restarts CoNest Host; tool subprocesses start only when needed."
No separate Manager process, no separate mandatory DSH Host, no arrow through Management on every call, no clients drawn inside Management, no decorative icons. Predominantly PURE WHITE, precise thin black borders, flat sky blue #75C7EE, green #A5DCB8, yellow #F5DA88, coral #EFAFA3 headers. Black readable text, evenly aligned boxes.

Endpoint correction prompt:
Edit target: the supplied CoNest two-process diagram. Preserve all labels, boxes, colors, layout, grouping, and existing lines except these THREE arrow endpoint adjustments:
1. The blue Studio / CLI "Manage" arrow currently stops at the outer CoNest Host border. Extend its right end through that border and end exactly on the LEFT border of the Management module white body, at the same y coordinate.
2. The top endpoint of the black "Calls / callbacks / events" connection currently stops at the outer Gateway border. Extend that horizontal segment left through the outer border, ending exactly at the RIGHT border of the inner CoNest Connector box, at the same y coordinate. The arrowhead must point into Connector.
3. The bottom endpoint of that same black connection currently stops at the outer CoNest Host border. Extend its horizontal segment left through the outer border, ending exactly at the RIGHT border of the Cordis Runtime module (not its inner CoNest Core), at the same y coordinate. The arrowhead must point into Runtime.
Keep the right-gutter vertical black line, its text and both endpoint arrowheads; it remains a single bidirectional connection that bypasses Management. Keep the blue Management -> Runtime arrow unchanged. Do not add any other arrows or change wording. Flat solid header fills, pure white bodies, thin black outlines.

Final validation:
Inspected final PNG. Both explicit process boundaries are correct. Management and Runtime share Host; DSH composition is optional and inside Runtime. The client arrow reaches Management. The black bidirectional connection reaches Connector and Runtime, bypassing Management. Core-without-DSH and external-supervision notes are present. Figure expresses target deployment, not current implementation.

Final PNG SHA256: e7c26a2dca24d7184b5d0525fe69bcbd99e63150419678c6f1ab06d938a78771
