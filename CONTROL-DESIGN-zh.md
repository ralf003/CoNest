# OpenClaw 与 DSH 插件生态统一控制及受控自进化方案

> 历史参考：最新建设目标、首阶段范围与实施顺序以 DESIGN-zh.md 为准。本文件保留风险分析和远期治理讨论，不代表相关功能已实现，也不要求迁移后优先建设自进化系统。

## 一、修订范围与建设目标

本方案以 OpenClaw 与 DSH 两侧能力的统一治理为目标，建立覆盖能力发现、任务授权、运行控制、经验积累和版本演进的连续处理机制。OpenClaw 保留用户交互、身份认证、宿主配置和原生运行时职责，Cordis 保留组件依赖与生命周期管理职责，DSH 保留智能体执行职责。统一控制体系负责协调上述职责，使每次能力调用和系统变更都具有明确的授权来源、执行归属、状态记录和恢复路径。

本次交付为架构修订及实施约束，不代表新增能力已经实现。已打包的 0.0.1 源码保持冻结，本文件随迁移资料单独交接。源码包中原技术说明有关“多个插件身份共用 Context 即可统一治理”“单个组件失败不影响其他组件”“后续扩展均无需宿主接口变化”的推断，应以本方案给出的实现条件和验收要求为准。

系统的自主性以预授权范围内的持续解决能力为衡量对象。Agent 可以识别故障、调用已有能力、检索经验证的经验、提出变更、运行隔离实验，并在明确的发布条件下自动部署和回退。人工主要决定目标、信任范围和风险政策；范围内的例行恢复无需逐次确认，扩大权限或改变控制规则则进入独立决策流程。

## 二、现有实现的结构性不足

现有 Bridge 已经贯通工具代理、子任务委托和完整 Harness 三种运行方式，但这些方式尚未由相同的调用授权和生命周期对象约束。`src/index.ts` 中的委托入口向 `runAgent` 传递任务、模型、会话、超时和取消信号；`src/dsh-agent-harness.ts` 中的完整 Harness 额外传递工具策略和宿主审批接口。直接工具路径则使用宿主创建的简化 Agent 对象。入口工具获得调用许可，不能据此推导其内部委托获得了所有工具的使用许可。此处确认的是适配器的策略传递差异，完整越权结论仍需通过宿主实际准入和嵌套调用复现。

运行对象同样存在粒度不足。Bridge 使用单一 `CordisBridgeHost`，在固定组合中集中启动模型、工具、MCP、会话和沙箱。启动校验主要比较工具名称列表，尚未绑定完整参数模式、依赖版本和权限声明。配置中的工作目录决定该组合的文件、附件和会话位置，不能仅凭不同会话标识推导多个工作区或信任域已经隔离。源码中的模块级单例只在所在进程内成立，也不能保证 Gateway 与 Harness 工作进程之间实际共享同一实例。

当前控制页面承担状态展示职责。其组件分类来自静态列表，主要状态来自 Bridge 生命周期和最近一次运行记录；这些数据不能替代组件级健康检查、目标配置、变更执行记录和故障恢复状态。活动记录采用 JSONL，读取时先加载完整文件，再选择末尾记录，因此末尾记录数量限制没有形成文件读取内存上限。这是持续运行前需要处理的具体落差，但不在本次文档修订中修改实现。

当前自动记忆用于保存明确的用户偏好和决定，具备长度限制及部分不可信内容处理，但尚未形成工程经验管理。哈希实体名用于组织记录，不构成访问控制；显式 MCP 图谱工具与自动记忆共用后端，需要在所有读写入口执行一致的主体校验。主体字段不足时退回 Agent 范围的行为，也需要在多用户场景中重新确定。经验、策略、审计和会话数据不能因共用“记忆”名称而采用同一信任等级。

DSH 已提供动态插件的定义、运行、更新、停止和检查机制，可复用其插件、包版本和运行实例概念。然而，该机制的注册表与版本指针主要保存在进程内，动态代码所用的 `node:vm` 不提供恶意代码安全隔离。Bridge 当前固定组合也没有装载这些动态插件组件。将相应工具直接加入生产组合，既不能补齐持久化发布流程，也不能证明未来版本沿用既有授权是安全的。

## 三、统一控制的职责边界

统一控制采用共同对象模型和单一变更入口，同时保留资源各自的唯一写入方。OpenClaw 原生插件、宿主身份、会话准入和模型选择继续由 OpenClaw 管理；DSH 组件实例由受监督的 Cordis 运行进程管理；协调器负责变更计划、授权约束、执行跟踪和故障恢复。协调器通过受支持接口请求资源变更，不直接改写宿主内部数据库，也不与原生管理器竞争同一资源。

对尚无受支持控制接口的 OpenClaw 资源，统一目录应明确报告只读或不支持自动变更。后续可以通过宿主认可的维护命令执行有计划的切换；确需新的公共接口时，应提出通用扩展接口需求并单独验证。不得以绕过宿主注册、直接写私有状态或伪造插件身份的方式实现表面上的统一。

统一控制与执行采用两个信任等级。稳定的协调器、策略判定和发布凭据位于 Agent 不可写入的控制进程及存储范围；可扩展的 Cordis/DSH 执行进程承载任务和候选组件。OpenClaw 插件作为薄适配层，通过经认证的本地接口接入协调器，不把任意模型生成代码加载进 Gateway。首阶段采用单机单写者，无需引入 Kubernetes、消息集群或分布式数据库；独立进程承担的是隔离和恢复职责。

运行进程按信任域、工作区和组合版本分组。相同授权范围内可以共享模型连接、MCP 客户端和只读依赖；不同主体、凭据范围或候选版本应使用独立组。Cordis 的子 Context 和 Fiber 用于依赖管理及资源释放，不代替操作系统进程、文件和网络隔离。候选代码能够访问的服务本身也必须受限，不能通过一个可调用服务重新获得控制进程的全部权限。

上述职责需要共同的数据对象支撑。主要对象及其归属如下表所示。

| 对象 | 主要内容 | 唯一归属与作用 |
| --- | --- | --- |
| 能力描述 | 稳定标识、提供方、模式摘要、作用对象、副作用、兼容要求 | 目录记录声明及验证结果，不自行授予权限 |
| 组合修订 | 不可变源码及构建摘要、依赖锁、能力集合、配置摘要、状态格式 | 协调器保存目标版本；执行进程报告实际版本 |
| 执行授权 | 宿主确认的主体、任务、父子关系、范围、撤销状态 | 策略与宿主权威对象共同约束每次调用 |
| 操作记录 | 幂等键、预期修订、执行阶段、结果及待核查状态 | 资源执行方记录事实，协调器跟踪完成与恢复 |
| 经验条目 | 来源、适用条件、证据、反例、状态及版本 | 经验管理器控制准入与撤销，执行器只读使用已准入内容 |
| 演进候选 | 变更目标、固定基线、评测结果、权限差异及发布计划 | 候选生成、独立验证和激活分别执行 |

## 四、目标状态与实际状态的协调机制

控制接口应提交目标状态和预期版本，而不是直接暴露任意加载、卸载或执行命令。协调器先解析组合依赖、共享资源、互斥能力和受影响任务，形成可以预览的变更计划。只有依赖闭包、兼容条件、授权范围和恢复条件均满足时，计划才进入执行。插件声明中的只读、幂等或低风险标记属于待验证信息，不能直接用于免除检查。

运行状态至少区分目标修订、已观察修订、实例代次、准入情况、健康结果和等待原因。所有者使用版本条件更新及幂等操作键拒绝过期请求；执行进程需要具备可撤销的当前实例授权。相同资源只有一个变更所有者，人工通过原生入口改变资源时，协调器应识别冲突、暂停相关自动变更或接受新目标，不通过反复覆盖配置与人工竞争。

状态协调以事件触发为主，以有界周期核对作为漏事件恢复机制。控制进程重启后读取未完成操作，再查询资源的实际状态。已经完成的操作应补齐结果，尚未产生效果的操作可以安全重试，结果未知的操作进入核查。网络调用、PR 发布和消息发送等外部副作用无法仅靠本地事务获得恰好一次语义；应优先使用目标系统的幂等标识和结果查询，无法确认时不得盲目重复执行。

工作进程崩溃由独立监督者处理，恢复过程保持原任务和操作标识，递增实例代次并撤销旧实例权限。重试采用分类型退避和次数或时间边界，重复故障进入隔离状态并留下诊断事实。控制进程不可用时，停止新的配置变更；既有任务只能在原来明确有效的授权内继续，授权无法核实时停止新的高影响操作。维护入口、停止开关和最后已知可用版本不依赖候选插件正常运行。

## 五、三种执行方式的一致授权

工具代理、子任务委托和完整 Harness 应共用任务调用上下文。该上下文包含由宿主确认的主体、工作区、当前任务、父任务、组合修订、允许操作、凭据引用和取消关系。主体缺失或字段不可可靠转换时，相关能力应拒绝准入或明确降级为不含敏感数据的只读操作；不得落入跨用户共享的全局身份。

每次调用的有效权限由宿主授权、部署政策、任务范围、父任务权限和组件需求共同确定，委托权限只能收缩。策略需要约束文件路径、网络目标、记忆命名空间、模型访问和控制操作，不能只对工具名称取交集。支持 Shell 或可执行代码时，应以操作系统和服务端约束阻断底层访问；仅隐藏高风险工具名称无法阻止另一个执行工具完成相同操作。

授权检查同时覆盖准入与实际副作用边界。审批等待、异步排队和恢复之后，应重新检查当前任务、实例代次、取消状态及权限撤销。跨进程凭证应限定接收方、操作和有效期，并与宿主的当前权威对象绑定；复制的标识、旧审批结果或签名本身不应成为脱离运行实例的永久权限。Agent 无权通过修改参数扩大授权，也不能通过新建子任务绕过父任务限制。

持续授权应绑定允许的能力和资源范围，而不是绑定一个可以无限变化的插件名称。后续版本在原授权范围内且通过既定发布门槛时可自动运行；新增网络目的地、数据命名空间、凭据访问或不可逆副作用时，必须重新评估权限差异。宿主原有明确要求人工确认的规则继续生效，协调器不代替用户点击批准，也不将等待审批伪装为运行成功。

## 六、插件身份、动态能力与升级一致性

第一阶段保留一个 OpenClaw Bridge 插件身份，在其受控目录中建立具有稳定标识的子能力。只有在某项能力具备独立配置、授权执行、生命周期、健康检查和审计归属时，才考虑将其暴露为独立 OpenClaw 插件。没有宿主公共子插件作用域支持时，生成多个清单不足以证明隔离成立，展示应准确说明其仍受 Bridge 统一托管。

部署制品应固定依赖图、构建摘要、工具名称与参数模式、输出语义和权限需求。能力发现分为可信的静态描述校验和隔离运行探测；扫描阶段不得在控制进程内导入并执行未受信的插件包。当前从真实 Cordis 组合生成目录的做法可保留为可信构建步骤，但候选包必须在不含生产凭据的构建环境中运行。

OpenClaw 的同步注册要求应纳入发布事务。原生工具集合变化时，候选版本需要先生成完整清单并通过宿主加载检查，再在安全边界切换。每个任务固定一个组合修订和工具目录摘要；老任务使用老实例完成或明确终止，新任务才进入新实例。不能在同一轮模型已看到工具定义之后悄然替换实现及权限。

动态扩展需要区分宿主原生注册与内部任务能力。若经验证的公共接口支持按任务提供工具集合，可在任务准入阶段绑定该集合；否则保留静态宿主注册，并将实验能力限制在独立 DSH 候选运行中。通用调用代理可作为明确的后续接口选项，但它仍需逐能力校验参数、授权和审计，并验证其对模型可发现性和缓存的影响，不能用一个万能执行工具绕开宿主管理。

组件升级采用候选实例启动、健康验证、停止新任务进入旧实例、切换新任务路由、旧实例排空和释放的顺序。共享状态存在单写限制时，不允许新旧版本同时写入；需要兼容协议、隔离副本或维护窗口。回退也不能自动撤销已发生的外部操作，无法逆转的结果应采用补偿或人工核查。状态格式不支持旧版本读取时，不提供虚假的一键回滚承诺。

## 七、可靠记忆与工程经验复用

记忆体系应区分会话事实、工程经验和控制政策。会话事实保持原始来源与主体范围；工程经验描述适用条件、已验证方法和失败反例；控制政策由用户或受信管理流程维护。三者可以通过统一查询入口呈现，但其写入权限和准入规则分别执行。对话中的偏好以及插件返回的说明不得直接晋升为部署授权或安全政策。

工程经验由实际任务记录产生候选，经过证据检查后进入可复用集合。每条经验至少关联仓库或能力范围、依赖和接口版本、问题特征、操作步骤、可核验结果、负面条件、最近验证时间和替代关系。检索首先检查主体权限与适用条件，再进行相关性排序；版本不符、证据缺失或已经撤销的经验不得作为确定性操作依据。失败记录与成功记录共同用于后续判断，避免只积累成功叙述。

经验状态按候选、已验证、已停用和已替代管理。被引用的经验需要回写使用结果，以便发现过时规则和反复失败；纠错、撤销、去重及敏感数据删除必须传递到检索索引和缓存。候选生成 Agent 无权同时修改准入标准与验证结果。对同一语义事实的跨后端复制，应保持来源和版本，不能在多个记忆系统之间互相转述并增加虚假的可信度。

控制日志、已发布经验和评测证据应保存在执行工作区之外。原始 DSH 会话可继续由其原生持久化组件管理，宿主会话由 OpenClaw 管理；协调器只保存必要的关联和变更事实，避免两套会话正文成为相互冲突的权威记录。新增控制存储或跨版本迁移属于单独实施决策，本方案不授权直接修改现有 OpenClaw 数据库模式。

## 八、自进化的实验与发布闭环

自进化以稳定业务基线和受控实验为基础。系统先从实际失败、重复成本、人工介入原因和能力缺口中选择改进目标，确认已有组件或已验证经验是否足以解决问题。只有能够定义预期收益、影响范围和验收方法的目标才进入实验，避免把增加插件数量、修改代码次数或模型自评分数当作进步。

候选生成在独立环境中进行。Agent 可以修改候选组合、插件实现和经验提案，但不能修改生产控制器、凭据、评测基线及发布政策。DSH 的动态定义和运行机制适合承载临时原型；被选中的原型需要导出为常规源码和锁定制品，经过持久化验证流程后才能发布。浏览器动态扩展还需单独处理宿主同源权限与界面注入风险；第一阶段不开放候选代码直接进入生产 Control UI。

评测由候选不可写入的验证器执行，固定基线任务、权限约束、依赖范围和可接受退化条件。候选 Agent 可以添加复现用例，但发布判定还必须使用独立维护的回归与留出场景。模型评审可提供辅助解释，不能替代可执行断言。对任务成功率、无效重试、人工介入、端到端耗时、资源消耗和权限违规分别计量；安全约束不能用平均质量分数抵消。

实验流程由目标选择、候选生成、隔离验证、受限试运行、结果观察、激活或回退构成。影子运行使用脱敏数据、只读操作或副本，不能重复发送消息、发布 PR 或执行付款等外部写入。受限试运行绑定少量预授权任务、明确的停止条件和最后已知可用版本。结果不足、收益无法复现或出现质量退化时保留基线，允许自动修订候选，但限制实验次数与资源占用。

自主发布采用预先约定的风险分级。重启受管进程、使用已批准依赖、恢复已知可用组合等例行操作可以自动完成；不扩大权限、状态兼容且评测通过的组件或组合升级可以进入自动试运行与激活；增加权限、新增外部数据接收方、变更控制规则、修改验证器或实施不可逆状态迁移，需要独立批准。拒绝某类变更后应保留拒绝记录，不能换一个候选标识反复请求。

实验资源与业务资源分别调度。优先保证正在执行的用户任务，空闲资源才用于候选构建和评测；故障反复出现时停止实验并提交原因摘要。制品按内容摘要去重，共享只读依赖和构建缓存，候选使用可回收工作区；仍被任务、回退版本、有效经验或验证证据引用的对象受到保护。模型上下文和输出窗口使用所选原生 Agent 的默认配置，协调器管理的是并发、执行时限和实验次数，不能混同为随意缩小模型窗口。

## 九、实施顺序与验收条件

实施应先解决调用和状态的一致性，再开放动态变更。各阶段均交付一个可以验证的闭环，上一阶段的安全约束保留为后续回归门槛。阶段划分及退出条件如下表所示。

| 阶段 | 实施范围 | 退出条件 |
| --- | --- | --- |
| 基线核查 | 固定当前依赖、三路径行为、数据归属及接口支持矩阵 | 本机与目标机器结果可对应，未支持能力明确列出 |
| 一致授权 | 共用调用上下文、委托降权、工作区和记忆隔离、审批撤销 | 三路径对相同拒绝条件一致；取消后的等待操作不产生新副作用 |
| 可恢复控制 | 稳定控制进程、资源唯一所有者、持久化操作、实例隔离 | 控制器和执行器分别崩溃后可核对恢复，不重复外部写入 |
| 可验证经验 | 候选准入、版本适用性、反例、撤销传播 | 过期经验被拒绝，不同主体数据隔离，失效条目不再影响执行 |
| 受控演进 | 隔离候选、独立评测、试运行、激活与回退 | 坏候选自动停止，基线持续可用，权限扩大不能自动通过 |

故障验收至少覆盖：撤销审批后延迟返回的执行请求；父任务禁止的能力经子任务再次调用；两个主体访问同一记忆后端；工具模式在评测后发生变化；构建完成但激活前控制器崩溃；新实例启动后旧实例迟到写入；外部操作成功但本地回执丢失；共享依赖在排空时失效；新格式状态无法被旧版本读取；候选企图修改评测证据；没有浏览器响应的扩展请求；迁移后运行路径改变。每个场景应验证实际副作用、持久化状态和用户可见结果，不能只检查返回字段。

第一批实现应集中在委托授权一致性、主体与工作区隔离、权威操作记录及完整能力摘要。它们直接决定后续自动升级是否安全。多插件身份展示、任意生态扩展和生成式管理页面放在这些条件之后。最终验收应证明系统能够在授权范围内自行诊断、恢复、积累经验并改进受管组件，同时在证据不足或权限变化时给出明确的保留基线或等待决策结果。
