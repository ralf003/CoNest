# OpenClaw 可演进插件增强架构简明设计

日期：2026-09-06  
状态：初步设计，待迁移后实施验证；随包源码仍为既有 0.0.1 开发快照。

## 一、目标与范围

面向个人与组织使用场景，以 OpenClaw 为智能体基座，引入 Cordis 的组件组合与生命周期管理机制，建立支持插件持续新增、组合、替换和退出的增强架构，并分级兼容 DeepSeek Harness（DSH）插件生态。OpenClaw 保留用户入口、会话、任务执行、授权和消息交付职责，通过公开扩展接口接入增强能力，原则上不修改核心代码、不维护侵入式分叉。

本阶段的“生态演进”指插件及其依赖关系能够持续变化，并保持运行状态可观察、变更行为可验证。AI 自动生成插件、修改核心代码和自主发布不属于首阶段范围。论文机制与工程实现的对应关系，仍需取得并核对所指硕士论文；本方案不将未核实的理论保证直接用于产品承诺。

## 二、总体架构

架构由 OpenClaw 基座、OpenClaw 适配插件、Cordis 插件扩展层和统一管理入口构成。组件是安装与生命周期管理单位，能力是组件对外提供的服务或操作，执行器是运行主 Agent Loop 的实现。三者分别建模，避免能力必须依附于某个 Loop 才能使用。

OpenClaw 适配插件通过公开接口映射任务上下文、工具调用、受支持事件和结果交付。Cordis 插件扩展层承载新增组件及可兼容的 DSH 组件，负责依赖就绪、作用范围、注册资源和释放过程。优先评估 Gateway 外独立进程部署，MCP/RPC 用于跨进程通信；具体传输与事件契约在接口验证后确定。通信接口必须表达调用身份、运行有效期、取消和连接失效，不能把远端服务视为无故障的本地对象。

统一管理入口提供组件清单、配置、版本、依赖、健康状态和受支持的管理操作。Cordis 完整管理其扩展域内的组件；既有 OpenClaw 插件继续由原生机制加载，适配层仅通过公开接口进行协同管理。统一登记不等于取得原生插件的生命周期控制权。首阶段提供命令行与必要状态输出，不建设 Web 产品。

## 三、插件接入与任务组合

新增组件应声明服务依赖、对外能力、输入输出约定、权限需求、支持版本及资源释放方式。优先复用 Cordis 和 DSH 现有包、装配机制与约定，只补充宿主适配和治理所需信息。支持统一安装入口，但不强制不同宿主使用同一个物理目录。

DSH 兼容按运行依赖分级推进：优先接入独立工具和通用服务；对依赖 DSH 服务接口的组件，复用相应服务包或提供经过验证的适配；对依赖特定会话、推理事件、执行器或 UI 的组件，明确支持条件和暂不支持范围。相同的工具名称不构成兼容证明，需验证依赖与事件语义。

任务默认由一个选定的主 Loop 组织，在当前任务获授权且运行就绪的能力中按需选择和组合。OpenClaw 原有能力与 Cordis 扩展能力经适配后参与同一任务；普通服务调用不隐式启动第二个 Agent Loop。Codex、Claude Code、DSH 等执行器通过各自受支持接口接入，不承诺所有原生插件均能跨执行器使用。能力记录保留原始提供方，防止代理被重复导出形成循环调用。

## 四、生命周期与安全约束

组件只有在必需依赖就绪后才能激活；依赖消失时，其消费者按约定停止并释放注册资源，依赖恢复后再恢复服务。升级先检查候选版本与依赖兼容性，再将新任务切换至新版本，旧任务完成或明确终止后释放旧实例。任务固定组件版本与能力描述，不在执行中静默替换实现；不支持热更新的宿主采用明确的重载或维护流程。

跨进程调用在服务端验证任务身份、权限范围及授权有效期，取消或结束后回收调用资格。所有入口执行一致权限规则，避免 DSH 原生工具绕过 OpenClaw 限制。超时后区分执行失败与结果未知，有外部副作用的操作不得盲目重试。组件卸载负责受管理资源清理，不能自动撤销已发送消息、已提交 PR 或不可逆数据迁移。

个人侧采用单用户工作区、预设组合和低配置恢复；组织侧逐步增加插件准入、固定版本、发布审核和审计。两类场景共用接口与运行机制。Cordis 的服务作用域不等于安全沙箱，多用户和不可信插件的文件、凭据与网络访问需要独立隔离措施。

## 五、现有代码复用与实施顺序

随包 bridge 已具备固定 DSH 组合、工具转发、记忆接入和 DSH Harness，是后续适配实现的基础。当前仍存在固定装配、生成式静态工具清单、能力宿主与 Loop 耦合以及入口策略传递差异；尚未实现本方案的统一组件管理、独立扩展进程与分级生态兼容。

迁移后先恢复冻结依赖并复现已有测试，再核查目标 OpenClaw 发布版本的公开接口及 Cordis/DSH 服务契约。随后拆分能力宿主与可选 Loop，建立任务级适配和独立进程的生命周期协议；最后加入组件管理与兼容测试。缺少公开扩展点的能力先记录限制，确有必要时提出通用上游接口需求，不通过内部导入或运行时补丁绕过边界。

## 六、首阶段验收

以知识检索与结果验证为示范：OpenClaw 发起任务，Cordis 承载一个兼容接入的 DSH 检索服务，再安装一个依赖该服务的验证插件，由主 Loop 组合使用并通过 OpenClaw 交付结果。验收应满足以下条件：

1. 新增符合约定的插件无需修改连接层主代码，即可配置、发现和调用。
2. 必需依赖缺失时不错误激活；依赖恢复、组件停用和替换后的状态可验证。
3. 卸载后工具注册与事件监听得到清理，无关组件和 OpenClaw 原有功能仍可使用。
4. 权限拒绝、任务取消、进程故障和升级失败有实际行为测试，不仅检查返回字段。
5. 至少验证一个有真实服务依赖的 DSH 插件，并记录支持版本与兼容限制。
6. 在未修改的 OpenClaw 发布包上通过验证；明确区分历史源码测试和新版本验收结果。

本文件是迁移后的最新建设方向。CONTROL-DESIGN.md 保留为历史风险与远期治理参考，不能据此将自进化、经验系统或自主发布提前纳入首阶段交付。
